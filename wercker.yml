box: mies/meteor
build:
  steps:
     - script:
          name: echo nodejs information
          code: |
            echo "node version $(node -v) running"
            echo "npm version $(npm -v) running"
    - script:
          name: install laika
          code: npm install laika
    - script:
          name: run laika
          code: laika
